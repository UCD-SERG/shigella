% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_posterior_at_times}
\alias{predict_posterior_at_times}
\title{Predict Antibody Levels at Specified Times from Posterior Draws}
\usage{
predict_posterior_at_times(
  model,
  ids,
  antigen_iso,
  times = seq(0, 200, by = 1)
)
}
\arguments{
\item{model}{A fitted individual-level serodynamics model object, with
columns \code{Subject}, \code{Iso_type}, \code{Chain}, \code{Iteration}, \code{Parameter},
and \code{value}.}

\item{ids}{Character vector of subject IDs to predict for.}

\item{antigen_iso}{A character scalar identifying the isotype to filter
(e.g., \code{"IgG"}, \code{"IgA"}).}

\item{times}{Numeric vector of time points (in days since symptom onset)
at which to evaluate the antibody trajectory. Default \code{seq(0, 200, 1)}.}
}
\value{
A long tibble with one row per draw x time point combination,
including columns \code{id}, \code{t}, \code{Chain}, \code{Iteration}, and \code{res}
(predicted antibody level on the natural MFI scale).
}
\description{
For one or more individuals and a single antigen-isotype combination,
evaluates the two-phase antibody kinetic model at each requested time
point across all MCMC posterior draws.
}
\examples{
\dontrun{
preds <- predict_posterior_at_times(
  model       = overall_IpaB_pop_6,
  ids         = "SOSAR-22008",
  antigen_iso = "IgG",
  times       = seq(0, 200, by = 5)
)
head(preds)
}

}
