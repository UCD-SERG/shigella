% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{build_table4}
\alias{build_table4}
\title{Build MAE Comparison Table Across Modeling Approaches}
\usage{
build_table4(
  mae_overall,
  mae_serospec = NULL,
  mae_combined = NULL,
  n_lookup = NULL
)
}
\arguments{
\item{mae_overall}{A tibble of per-individual MAE from the overall model,
as returned by \code{\link[=get_mae]{get_mae()}}, with columns \code{sid}, \code{antigen}, \code{Iso_type},
\code{mae}.}

\item{mae_serospec}{A tibble of per-individual MAE from serotype-specific
models, or \code{NULL}.}

\item{mae_combined}{A tibble of per-individual MAE from the combined
\emph{S. flexneri} model, or \code{NULL}.}

\item{n_lookup}{A tibble with columns \code{antigen} and \code{n} specifying the
sample sizes to display.  If \code{NULL}, sample sizes are computed from
the union of all input MAE tables.}
}
\value{
A \code{flextable} object ready for embedding in a Word document or
Quarto PDF, with columns \code{Antigen}, \code{Isotype}, \code{n}, \code{Overall},
\code{Sero-specific}, \verb{Combined flexneri}, \verb{Best model}.  Values are
\code{"median (Q1\\u2013Q3)"} strings on the log-MFI scale.
}
\description{
Compares per-individual prediction accuracy (median absolute error, MAE)
across overall, serotype-specific, and combined \emph{S. flexneri} models for
each antigen–isotype combination.  The best model is determined by counting
individual-level "wins" among participants shared across all compared models.
}
\details{
The best model for each antigen–isotype is determined as follows:
\enumerate{
\item Identify participants shared across all models being compared.
\item For each shared participant, record which model yields the lowest
MAE.
\item The model with the most individual-level wins is declared the best.
\item The \verb{Best model} column reports the winning model name and
\code{"wins / n_shared"} in parentheses.
}
When only one model is available for an antigen–isotype combination,
it is returned as the best model with the total number of participants.
}
\examples{
\dontrun{
tbl <- build_table4(
  mae_overall  = mae_overall,
  mae_serospec = mae_serospec,
  mae_combined = mae_combined,
  n_lookup     = n_lookup
)
tbl  # prints in RStudio viewer
}

}
