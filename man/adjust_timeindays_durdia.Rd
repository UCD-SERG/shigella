% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust_timeindays_durdia.R
\name{adjust_timeindays_durdia}
\alias{adjust_timeindays_durdia}
\title{Shift observation times by pre-presentation diarrhea duration ("_new")}
\usage{
adjust_timeindays_durdia(
  case_data,
  durdia,
  id_col = "id",
  durdia_id_col = "CaseID",
  hours_col = "DurDia_hours"
)
}
\arguments{
\item{case_data}{Case data from \code{\link[=process_shigella_data]{process_shigella_data()}}.}

\item{durdia}{A data frame with one row per participant containing the id and
diarrhea-duration columns.}

\item{id_col}{Name of the participant id column in \code{case_data} (default
\code{"id"}, the standardised id from \code{\link[=process_shigella_data]{process_shigella_data()}}; \code{"sid"} is
equivalent since the two columns are identical).}

\item{durdia_id_col}{Name of the id column in \code{durdia}. Default \code{"CaseID"},
confirmed against the "Duration of symptoms" sheet.}

\item{hours_col}{Name of the diarrhea-duration-in-hours column in \code{durdia}
(default \code{"DurDia_hours"}, confirmed).}
}
\value{
\code{case_data} with added columns \code{DurDia_hours}, \code{DurDia_days},
\code{timeindays_new}, and with \code{timeindays} overwritten by \code{timeindays_new}.
Case-data attributes are preserved.
}
\description{
Implements the time correction used to build the \verb{dL_*_new} objects. Each
participant's whole trajectory is shifted forward by the duration of diarrhea
that preceded hospital presentation, moving \code{t = 0} closer to the true onset
of infection.
}
\details{
The transformation (validated against \code{dL_clean_sf2a} vs \code{dL_clean_sf2a_new}):
\enumerate{
\item merge \code{DurDia_hours} onto the case data by participant id (constant
per participant);
\item \code{DurDia_days = DurDia_hours / 24};
\item \code{timeindays_new = timeindays + DurDia_days} (added to every visit);
\item overwrite \code{timeindays} with \code{timeindays_new}.
}
}
