% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_natural_scale.R
\name{add_natural_scale}
\alias{add_natural_scale}
\title{Back-transform log-scale \code{mu.par} draws to the natural parameter scale}
\usage{
add_natural_scale(df, alpha_unit = c("per_year", "per_day"))
}
\arguments{
\item{df}{Output of \code{\link[=extract_mu_draws]{extract_mu_draws()}}.}

\item{alpha_unit}{\code{"per_year"} (default, for display tables) multiplies the
decay rate by 365; \code{"per_day"} leaves it in day^-1.}
}
\value{
\code{df} with the five \verb{*_natural} columns added.
}
\description{
Applies the manuscript back-transformation to each draw:
\itemize{
\item \code{y0_natural    = exp(y0)}
\item \code{y1_natural    = exp(y1) + exp(y0)} (the \code{y1} column is
\code{log(y1 - y0)})
\item \code{t1_natural    = exp(t1)}
\item \code{alpha_natural = exp(alpha) * k}
(k = 365 for per-year, 1 for per-day)
\item \code{rho_natural   = exp(shape) + 1}
}
}
