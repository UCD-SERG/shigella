% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_mu_draws.R
\name{extract_mu_draws}
\alias{extract_mu_draws}
\title{Extract population-mean (\code{mu.par}) posterior draws in wide form}
\usage{
extract_mu_draws(fit_object, antigen_label = NULL)
}
\arguments{
\item{fit_object}{An \code{sr_model} object from \code{serodynamics::run_mod_pop()}.}

\item{antigen_label}{Optional antigen label added as an \code{antigen} column,
used when stacking several antigens before summarising.}
}
\value{
A tibble with columns \code{Iteration}, \code{Chain}, \code{Iso_type},
\code{y0}, \code{y1}, \code{t1}, \code{alpha}, \code{shape} (and \code{antigen} if
\code{antigen_label} given).
}
\description{
Pulls the \code{mu.par} rows from a fitted model's \code{population_params} attribute
and pivots them so each row is one posterior draw (Iteration x Chain x
isotype) with one column per kinetic parameter on the \strong{log/JAGS scale}
(\code{y0}, \code{y1}, \code{t1}, \code{alpha}, \code{shape}).
}
