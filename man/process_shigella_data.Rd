% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_shigella_data.R
\name{process_shigella_data}
\alias{process_shigella_data}
\title{Build single-antigen case data from the compiled Shigella table}
\usage{
process_shigella_data(data, antigen, study_filter = "SOSAR", as_case = TRUE)
}
\arguments{
\item{data}{Compiled MFI table (the \code{Compiled} sheet): one row per
sample x isotype, containing \code{study_name}, \code{sid}, \code{timepoint},
\verb{Actual day}, \code{isotype_name}, \code{cohort_name}, \code{age}, and the antigen MFI
columns.}

\item{antigen}{<\code{\link[dplyr:dplyr_data_masking]{data-masking}}> The antigen MFI
column to extract, e.g. \code{n_ipab_MFI}.}

\item{study_filter}{Study to keep (default \code{"SOSAR"}, the Dhaka cohort).}

\item{as_case}{If \code{TRUE} (default) return a \code{serocalculator}
case-data object;
if \code{FALSE} return a plain tibble (useful for testing the reshape alone).}
}
\value{
Long case data with columns \code{id}, \code{antigen_iso}, \code{visit},
\code{timeindays}, \code{result}, \code{visit_num}, \code{cohort_name}, \code{age} (plus \code{sid},
\verb{Actual day}, and the antigen MFI column). \code{timeindays} equals \verb{Actual day}
at this stage; it is shifted later by \code{\link[=adjust_timeindays_durdia]{adjust_timeindays_durdia()}} for the
\verb{_new} datasets.
}
\description{
Filters the compiled MFI table to one study and one antigen and reshapes it
into the long "case data" layout expected by \code{serodynamics}/\code{serocalculator}.
Mirrors the original manuscript \code{process_shigella_data()}, and additionally
\strong{keeps \code{cohort_name} (infecting serotype) and \code{age}}, which live in the
compiled sheet (columns J and M; see the raw-data screenshot). Retaining them
lets the \verb{subset_*()} helpers below build the serotype-specific and
age-stratified datasets directly -- no separate metadata join is needed.
}
\details{
\strong{ID column.} The original helper created \code{index_id = sid} and attached
\code{ids_varname = "index_id"}. The shipped case-data objects (\code{dL_clean_sf2a}
etc.) and the current working data instead carry an \code{id} column, identical in
value to \code{sid}. To reproduce those objects exactly, this function
standardises the id column to \code{id} via \code{serodynamics::as_case_data()}
(\code{id_var = "id"}), rather than relying on any internal renaming. If a
downstream step needs \code{index_id}, change \code{id_var} here -- but the shipped
\code{.rda} inputs and the manuscript figures use \code{id}.

\strong{cohort_name / age.} The original bare object \code{dL_clean_sf2a} did not carry
these two columns, so the base (non-\verb{_new}) datasets built here are a
harmless superset of the originals (two extra metadata columns). This is
cosmetic: the model fit (\code{prep_data_new()} uses only
\code{id}/\code{antigen_iso}/\code{timeindays}/\code{result}) and the figures are unaffected,
and keeping the columns is what makes the serotype / age subsets
reproducible from one function.
}
