% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_data.R
\name{process_shigella_data}
\alias{process_shigella_data}
\title{Process Raw Shigella Luminex Data into Case-Data Format}
\usage{
process_shigella_data(data, study_filter, antigen_col)
}
\arguments{
\item{data}{A data frame containing the compiled Shigella Luminex data
(e.g., loaded from the compiled spreadsheet, sheet \code{"Compiled"}).
Must contain columns \code{study_name}, \code{isotype_name}, \code{sid}, \code{timepoint},
\verb{Actual day}, \code{cohort_name}, and the antigen column specified by
\code{antigen_col}.}

\item{study_filter}{A character string identifying the study to retain
(e.g., \code{"SOSAR"}).}

\item{antigen_col}{An unquoted column name (tidy-select style) for the
MFI variable to extract (e.g., \code{n_ipab_MFI}).}
}
\value{
A \code{case_data} object with columns \code{index_id}, \code{antigen_iso},
\code{visit}, \code{timeindays}, \code{result}, and \code{cohort_name}.
}
\description{
Filters a compiled Shigella dataset to a single study, selects a given
antigen MFI column, restructures it into long per-visit format, and
converts it to a case-data object.
}
\examples{
\dontrun{
library(readxl)
df <- read_excel("path/to/compiled_data.xlsx", sheet = "Compiled")
dL_ipab <- process_shigella_data(df, "SOSAR", n_ipab_MFI)
}

}
