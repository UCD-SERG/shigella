% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_and_save.R
\name{fit_and_save}
\alias{fit_and_save}
\title{Fit a population model and save the result}
\usage{
fit_and_save(
  data,
  name,
  object_name = name,
  settings,
  priors,
  dir,
  with_post = TRUE
)
}
\arguments{
\item{data}{Data frame of longitudinal immunological observations.}

\item{name}{Character. File stem; output is written as \verb{<name>.rda} in \code{dir}.}

\item{object_name}{Character. Variable name stored inside the \code{.rda}.
Defaults to \code{name}. Script 05 passes \code{"fit_obj"} to match the S3-table
loader contract in \code{R/tables_supp.R}.}

\item{settings}{Named list of MCMC settings with elements \code{nchain}, \code{nadapt},
\code{nburn}, \code{nmc}, \code{niter}. Required; no default to avoid dependency on globals.}

\item{priors}{Named list of prior hyperparameters with elements
\code{mu_hyp_param}, \code{prec_hyp_param}, \code{omega_param}, \code{wishdf_param},
\code{prec_logy_hyp_param}. Required; no default to avoid dependency on globals.}

\item{dir}{Character. Output directory for the \code{.rda} file. Required; no
default to avoid dependency on globals.}

\item{with_post}{Logical. Whether to compute posterior summaries. Default
\code{TRUE}.}
}
\value{
The fitted model object, invisibly.
}
\description{
Runs \code{serodynamics::run_mod_pop()} with the given data and MCMC
settings, then saves the fitted object to an \code{.rda} file. The object inside
the file is named \code{object_name} so that downstream \code{load()} calls find the
expected variable name.
}
