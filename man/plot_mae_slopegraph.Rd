% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supplemental_figures.R
\name{plot_mae_slopegraph}
\alias{plot_mae_slopegraph}
\title{Per-Individual MAE Slopegraph Across Modelling Approaches (S4 Fig)}
\usage{
plot_mae_slopegraph(mae_overall, mae_serospec = NULL, mae_combined = NULL)
}
\arguments{
\item{mae_overall}{A tibble of per-individual MAE from the overall model,
as returned by \code{\link[=get_mae]{get_mae()}}.}

\item{mae_serospec}{A tibble of per-individual MAE from serotype-specific
models, or \code{NULL}.}

\item{mae_combined}{A tibble of per-individual MAE from the combined
\emph{S. flexneri} model, or \code{NULL}.}
}
\value{
A \code{ggplot} object with \code{facet_wrap(~ biomarker)}, two panels per
row.  Only participants shared across all compared models within each
panel are plotted.
}
\description{
Produces a faceted slopegraph where each line connects one individual's
MAE across the overall, serotype-specific, and (where applicable) combined
\emph{S. flexneri} models.  Green lines indicate that the alternative model
reduced prediction error; grey lines indicate the overall model performed
better.  Corresponds to S4 Fig in the Chapter 1 manuscript.
}
\examples{
\dontrun{
p_slope <- plot_mae_slopegraph(
  mae_overall  = mae_overall,
  mae_serospec = mae_serospec,
  mae_combined = mae_combined
)
print(p_slope)
}

}
