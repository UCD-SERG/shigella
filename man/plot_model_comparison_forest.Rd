% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest.R
\name{plot_model_comparison_forest}
\alias{plot_model_comparison_forest}
\title{Forest Plot of Population-Level Kinetic Parameters Across Modelling Approaches}
\usage{
plot_model_comparison_forest(
  pop_overall,
  pop_serospec,
  pop_combined = NULL,
  colors = c(Overall = "#2563EB", `Serotype-specific` = "#DC2626", `Combined flexneri` =
    "#16A34A"),
  point_size = 3,
  line_width = 0.8,
  dodge_width = 0.5
)
}
\arguments{
\item{pop_overall}{A summarised parameter tibble (from
\code{\link[=summarise_pop_params]{summarise_pop_params()}}) for the overall model (n = 48), containing all
five antigens.}

\item{pop_serospec}{A summarised parameter tibble for the serotype-specific
models (\emph{S. flexneri} 2a, 3a, and \emph{S. sonnei}).}

\item{pop_combined}{A summarised parameter tibble for the combined
\emph{S. flexneri} model (n = 25), or \code{NULL} (default) to omit this group.}

\item{colors}{A named character vector of hex colors for the three model
groups.  Names must match \code{"Overall"}, \code{"Serotype-specific"}, and
\code{"Combined flexneri"}.}

\item{point_size}{Numeric.  Size of the posterior median point.
Default \code{3}.}

\item{line_width}{Numeric.  Width of the credible-interval line.
Default \code{0.8}.}

\item{dodge_width}{Numeric.  Vertical dodge between model groups on each
antigen row.  Default \code{0.5}.}
}
\value{
A \code{patchwork} object combining five parameter panels stacked
vertically, with a shared legend at the bottom of the last panel.
}
\description{
Produces a multi-panel forest plot (one panel per kinetic parameter)
comparing posterior medians and 95\% credible intervals from the overall,
serotype-specific, and (optionally) combined \emph{S. flexneri} hierarchical
models.  Corresponds to S6 Fig in the Chapter 1 manuscript.
}
\details{
Each panel corresponds to one kinetic parameter:
\describe{
\item{Panel 1}{Baseline antibody level \eqn{y_0}}
\item{Panel 2}{Peak antibody level \eqn{y_1}}
\item{Panel 3}{Time to peak \eqn{t_1} (days)}
\item{Panel 4}{Decay rate \eqn{\alpha} (year\eqn{^{-1}})}
\item{Panel 5}{Decay shape \eqn{\rho}}
}
Parameters \eqn{y_0}, \eqn{y_1}, \eqn{t_1}, and \eqn{\alpha} are
plotted on a log10 scale; \eqn{\rho} is plotted on a linear scale.
Within each panel, rows represent antigens and points are colour-coded
by model group.
}
\examples{
\dontrun{
library(dplyr)
library(patchwork)

pop_ov <- bind_rows(
  prep_pop_params(overall_IpaB_pop_6,   "IpaB"),
  prep_pop_params(overall_Sf2a_pop_6,   "Sf2a"),
  prep_pop_params(overall_Sf3a_pop_6,   "Sf3a"),
  prep_pop_params(overall_Sf6_pop_6,    "Sf6"),
  prep_pop_params(overall_Sonnei_pop_6, "Sonnei")
) |> transform_pop_params() |> summarise_pop_params()

pop_sp <- bind_rows(
  prep_pop_params(serotype_sf2a_3,   "Sf2a"),
  prep_pop_params(serotype_sf3a_3,   "Sf3a"),
  prep_pop_params(serotype_sonnei_3, "Sonnei")
) |> transform_pop_params() |> summarise_pop_params()

pop_cmb <- bind_rows(
  prep_pop_params(combined_flexneri_sf2a, "Sf2a"),
  prep_pop_params(combined_flexneri_sf3a, "Sf3a")
) |> transform_pop_params() |> summarise_pop_params()

p_forest <- plot_model_comparison_forest(
  pop_overall  = pop_ov,
  pop_serospec = pop_sp,
  pop_combined = pop_cmb
)
print(p_forest)
}

}
