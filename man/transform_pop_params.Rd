% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics.R
\name{transform_pop_params}
\alias{transform_pop_params}
\title{Transform Population Parameters from Log Scale to Natural Scale}
\usage{
transform_pop_params(pop_params)
}
\arguments{
\item{pop_params}{A tibble as returned by \code{\link[=prep_pop_params]{prep_pop_params()}}, with
columns \code{y0}, \code{y1}, \code{t1}, \code{alpha}, \code{rho} on the log scale.}
}
\value{
The same tibble with five additional columns:
\describe{
\item{\code{y0_natural}}{Baseline antibody level (MFI): \code{exp(y0)}}
\item{\code{y1_natural}}{Peak antibody level (MFI): \code{exp(y1) + exp(y0)}}
\item{\code{t1_natural}}{Time to peak (days): \code{exp(t1)}}
\item{\code{alpha_year}}{Decay rate (year\eqn{^{-1}}): \code{exp(alpha) * 365}}
\item{\code{rho_natural}}{Decay shape: \code{exp(rho) + 1}}
}
}
\description{
Converts the log-scale MCMC draws produced by \code{\link[=prep_pop_params]{prep_pop_params()}} to
natural-scale kinetic parameters suitable for biological interpretation
and plotting.
}
\details{
The model stores parameters as:
\itemize{
\item \code{y0}    = log(baseline)
\item \code{y1}    = log(peak - baseline)  → peak = exp(y1) + exp(y0)
\item \code{t1}    = log(time to peak in days)
\item \code{alpha} = log(decay rate in days\eqn{^{-1}}) → convert to year\eqn{^{-1}}
\item \code{rho}   = log(shape - 1)  → shape = exp(rho) + 1
}
}
\examples{
\dontrun{
draws <- prep_pop_params(overall_IpaB_pop_6, "IpaB")
draws_nat <- transform_pop_params(draws)
summary(draws_nat$y1_natural)
}

}
