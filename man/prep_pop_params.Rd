% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics.R
\name{prep_pop_params}
\alias{prep_pop_params}
\title{Extract and Reshape Population-Level Kinetic Parameters from a Fitted Model}
\usage{
prep_pop_params(fit_object, antigen_label)
}
\arguments{
\item{fit_object}{A fitted model object produced by the serodynamics
hierarchical MCMC workflow. Must have an attribute \code{"population_params"}
with columns \code{Population_Parameter}, \code{Iso_type}, \code{Parameter}, \code{value},
\code{Iteration}, and \code{Chain}.}

\item{antigen_label}{A character scalar used to label this antigen in the
resulting data frame (e.g., \code{"IpaB"}, \code{"Sf2a"}).}
}
\value{
A tibble with one row per MCMC draw (chain × iteration), and
columns \code{Iteration}, \code{Chain}, \code{antigen}, \code{Iso_type}, \code{y0}, \code{y1}, \code{t1},
\code{alpha}, \code{rho}. All parameter values are on the \strong{log scale} as stored
internally by the model; use \code{\link[=transform_pop_params]{transform_pop_params()}} to convert to the
natural scale.
}
\description{
Pulls the \code{"mu.par"} population-level draws from the \code{population_params}
attribute of a fitted serodynamics model object, pivots them to wide
format, and attaches an \code{antigen} label column.
}
\examples{
\dontrun{
draws <- prep_pop_params(overall_IpaB_pop_6, "IpaB")
head(draws)
}

}
