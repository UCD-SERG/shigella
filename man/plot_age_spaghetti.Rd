% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supplemental_figures.R
\name{plot_age_spaghetti}
\alias{plot_age_spaghetti}
\title{Age-Stratified Spaghetti Plot of Raw Antibody Responses (Supplemental)}
\usage{
plot_age_spaghetti(
  dL_list,
  age_label = "",
  col_map = c(Sf2a = "#D81B60", sonnei = "#1E88E5", Sf3a = "#E65100", Sf6 = "#2E7D32",
    Other = "grey45"),
  alpha_map = c(Sf2a = 0.45, sonnei = 0.4, Sf3a = 0.5, Sf6 = 0.45, Other = 0.2),
  lw_map = c(Sf2a = 0.5, sonnei = 0.4, Sf3a = 0.45, Sf6 = 0.4, Other = 0.2)
)
}
\arguments{
\item{dL_list}{A named list of \code{case_data} objects, one per antigen,
in the order \code{IpaB}, \code{Sf2a}, \code{Sf3a}, \code{Sf6}, \code{Sonnei}.  Each object
must have \code{sid} (or \code{id}), \code{timeindays}, \code{result}, \code{isotype_name}
(or \code{antigen_iso}), and \code{cohort_name} columns.}

\item{age_label}{Character scalar used as the plot subtitle, e.g.,
\code{"<5 years"} or \code{"\\u22655 years"}.}

\item{col_map}{Named character vector of hex colours for infecting
serotypes.  Default matches the main spaghetti figure.}

\item{alpha_map}{Named numeric vector of line alpha values per serotype.
Default matches the main spaghetti figure.}

\item{lw_map}{Named numeric vector of line widths per serotype.
Default matches the main spaghetti figure.}
}
\value{
A \code{ggplot} object with \code{facet_grid(antigen ~ isotype_name)}.
}
\description{
Produces a faceted spaghetti plot of raw longitudinal IgG and IgA antibody
trajectories for one age subgroup (either children <5 or ≥5 years),
coloured by infecting serotype.  Used for S1 Fig (under-5) and S2 Fig
(plus-5) in the Chapter 1 manuscript.
}
\details{
The function calls \code{.bind_antigen_list()} internally to combine the named
list into a single long tibble before plotting.
}
\examples{
\dontrun{
data("dL_clean_Ipab_new_under5",   package = "shigella")
data("dL_clean_sf2a_new_under5",   package = "shigella")
data("dL_clean_sf3a_new_under5",   package = "shigella")
data("dL_clean_sf6_new_under5",    package = "shigella")
data("dL_clean_sonnei_new_under5", package = "shigella")

dL_under5 <- list(
  IpaB   = dL_clean_Ipab_new_under5,
  Sf2a   = dL_clean_sf2a_new_under5,
  Sf3a   = dL_clean_sf3a_new_under5,
  Sf6    = dL_clean_sf6_new_under5,
  Sonnei = dL_clean_sonnei_new_under5
)
plot_age_spaghetti(dL_under5, age_label = "<5 years")
}

}
\seealso{
\code{\link[=fig5_with_individuals]{fig5_with_individuals()}}, \code{\link[=build_figure4_row]{build_figure4_row()}}
}
