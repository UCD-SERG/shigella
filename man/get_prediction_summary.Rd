% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{get_prediction_summary}
\alias{get_prediction_summary}
\title{Summarise Posterior Predictions for a Single Subject and Isotype}
\usage{
get_prediction_summary(model_output, sid, iso, times = seq(0, 200, by = 1))
}
\arguments{
\item{model_output}{A fitted model object (same format as \code{model} in
\code{\link[=predict_posterior_at_times]{predict_posterior_at_times()}}).}

\item{sid}{A single character subject ID.}

\item{iso}{A character scalar isotype (\code{"IgG"} or \code{"IgA"}).}

\item{times}{Numeric vector of prediction times (days). Default
\code{seq(0, 200, by = 1)}.}
}
\value{
A tibble with columns \code{t}, \code{med}, \code{lo}, \code{hi} (posterior median,
2.5\%, and 97.5\% quantiles of predicted MFI at each time point).
Returns an empty tibble with a warning if no data are found.
}
\description{
A lightweight wrapper around \code{\link[=predict_posterior_at_times]{predict_posterior_at_times()}} that returns
the posterior median and 95\% credible interval of predicted antibody
levels at each time point.
}
\examples{
\dontrun{
pred <- get_prediction_summary(overall_IpaB_pop_6, "SOSAR-22008", "IgG")
}

}
